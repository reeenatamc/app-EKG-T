import { PlaygroundScreen } from '@/screens/PlaygroundScreen';

/**
 * Ruta del banco de pruebas de la capa de diseno.
 *
 * @returns El banco de pruebas.
 */
export default function PlaygroundRoute() {
  return <PlaygroundScreen />;
}
